# 不语博客操作流程

## 一、GitHub Pages 部署设置（首次）

### 1. 访问设置页面
打开浏览器访问：
https://github.com/zhanglijian2026/buyu/settings/pages

### 2. 配置部署设置
- **Source**: 选择 `Deploy from a branch`
- **Branch**: 选择 `main`
- **Folder**: 选择 `/ (root)`
- 点击 `Save`

### 3. 等待部署完成
- 页面会显示 "Your site is live at https://zhanglijian2026.github.io/buyu/"
- 等待 1-2 分钟让 GitHub 完成构建

### 4. 访问博客
部署成功后访问：
https://zhanglijian2026.github.io/buyu/

---

## 二、日常发布新文章

### 1. 创建新文章
在 `E:\不语\src\content\blog\` 目录下创建新的 `.md` 文件，例如 `my-new-post.md`

### 2. 文章格式
```markdown
---
title: '文章标题'
description: '文章简介'
pubDate: 2026-08-30
---

这里是文章内容...
```

### 3. 本地预览（可选）
```bash
cd E:\不语
npm run build
python -m http.server 8000 -d dist
```
访问 http://localhost:8000 预览

### 4. 提交并推送
```bash
cd E:\不语
git add .
git commit -m "Add new post: 文章标题"
git push
```

### 5. 等待自动部署
GitHub 会自动构建并部署，1-2 分钟后生效

---

## 三、更新站点配置

### 修改站点信息
编辑 `E:\不语\src\consts.ts`
- `SITE_TITLE`: 站点标题
- `SITE_DESCRIPTION`: 站点描述
- `SITE_AUTHOR`: 作者名称

### 修改导航菜单
编辑 `E:\不语\src\components\Header.astro`
- 修改导航链接和文字

### 修改样式
编辑 `E:\不语\src\styles\global.css`
- 修改颜色、字体等样式

### 提交更改
```bash
cd E:\不语
git add .
git commit -m "Update site config"
git push
```

---

## 四、部署流程图

```
写文章 → 提交Git → 推送GitHub → 自动构建 → 自动部署 → 网站更新
```

---

## 五、常用命令

```bash
# 进入项目目录
cd E:\不语

# 本地构建
npm run build

# 本地预览（开发模式）
npm run dev

# 提交更改
git add .
git commit -m "提交说明"
git push

# 查看状态
git status
git log
```

---

## 六、故障排除

### 部署失败
- 检查 GitHub Actions 标签页查看错误
- 本地运行 `npm run build` 测试构建是否正常

### 样式异常
- 清除浏览器缓存
- 检查 global.css 是否有语法错误

### 图片不显示
- 图片放在 `E:\不语\src\assets\` 或 `E:\不语\public\` 目录
- 使用相对路径引用

---

## 七、后续增强

### 1. 评论系统
- giscus (基于 GitHub Discussions)
- Waline

### 2. 搜索功能
- Astro 自带搜索插件

### 3. 深色/浅色主题切换
- 增加主题切换组件

### 4. 统计分析
- Google Analytics
- 百度统计